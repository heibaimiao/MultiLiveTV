# 已归档 — Next.js 网页端

此目录为历史 Next.js 全栈点播站，**已冻结**，不再维护。

当前主方向：

- **后端**：[`apps/api-go`](../apps/api-go/) — Go Gin API
- **客户端**：[`clients/apple`](../clients/apple/) — Apple TV + iPad

如需本地运行旧版网页（仅供参考）：

```bash
cd web
npm install
npm run dev
```

访问 http://localhost:3000
